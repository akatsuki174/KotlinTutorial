package Problem

import LongerExamples.next

/**
 * Created by akatsuki174 on 2017/07/14.
 */

/*
 * どんな配列であっても同じ数の並びが連なったrunの集合です。
 * 例えば以下の配列は2つのrunsを持っている。
 *   1, 1, 1, 2, 2
 * 列中の3つの1が最初のrunを構成し、2つの2が次のrunを構成している。
 * この配列には2つのrunがあります。
 *   3, 4
 * そしてこの配列には5つのrunがあります。
 *   1, 0, 1, 1, 1, 2, 0, 0, 0, 0, 0, 0, 0
 * 引数の配列にはいくつのrunが含まれているのかを返すメソッドを作る
 */

fun runs(a: IntArray): Int {
    return 0
}
